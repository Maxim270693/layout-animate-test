import React from 'react';
import LogoBlock from "../LogoBlock";

const Blog = () => {
    return (
        <div>
            <LogoBlock/>
            Blog Page
        </div>
    );
};

export default Blog;